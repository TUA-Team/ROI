using Terraria.ModLoader;

namespace ROI.Items
{
    /// <summary>
    /// Should be the base of every item, unless it doesn't make use of it
    /// </summary>
    public abstract class ROIItem : ModItem
	{
	}
}
